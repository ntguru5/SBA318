<!DOCTYPE html>
<html>
  <head>
    <title><%= pet.name %>'s Profile</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <h1><%= pet.name %>'s Profile</h1>
    <p>Breed: <%= pet.breed %></p>
    <p>Age: <%= pet.age %></p>
    <img src="/uploads/<%= pet.profilePicture %>" alt="Profile Picture">
    <form action="/pets/<%= pet.id %>?_method=PATCH" method="POST">
    <!-- <form action="/pets/<%= pet.id %>" method="patch" method="post"> -->
      <!-- <input type="hidden" name="_method" value="patch"> -->
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" value="<%= pet.name %>"><br><br>
      <label for="breed">Breed:</label>
      <input type="text" id="breed" name="breed" value="<%= pet.breed %>"><br><br>
      <label for="sex">Sex:</label>
      <input type="text" id="sex" name="sex" value="<%= pet.sex %>"><br><br>
      <label for="age">Age:</label>
      <input type="number" id="age" name="age" value="<%= pet.age %>"><br><br>
      <input type="submit" value="Update Pet">
    </form>
  </body>
</html>
